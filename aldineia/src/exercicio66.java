import java.util.Scanner;

/*
66) Faça um algoritmo que calcule a soma de todos os números 
ímpares dentro de uma faixa de valores determinada pelo usuário. 
Um número é ímpar quando sua divisão por 2 não é exata, ou seja, 
o resto resultante da divisão inteira pelo número 2 tem valor 1. 
Utilize a palavra resto como operador que calcula o resto da divisão
de um numero por outro.

SAIDA- calcule a soma de todos os números ímpares 

- Um número é ímpar quando sua divisão por 2 não é exata
- o resto da divisão inteira pelo número 2 tem valor 1. 
(numero % 2 == 1) numero impar
- USAR resto como operador que calcula o resto da divisão
de um numero por outro.


public class exercicio66 {
    public static void main(String[] args) {
        Scanner leitor = new Scanner(System.in);
        int numero = 0;
        int soma =0;
        int contador =0;
        int contapar = 0;
        int contaimpar = 0;
        int somapar = 0;
        int somaimpar = 0;

        do{
            System.out.println("Digite um numero");
            numero = Integer.parseInt(leitor.nextLine()); 
            if(numero % 2 == 1){
                somaimpar += numero; 
                contaimpar++;
            }
            contador++; 
        {while{
            System.out.println("A soma e " + soma);


        */            
    
        

        
    
    

